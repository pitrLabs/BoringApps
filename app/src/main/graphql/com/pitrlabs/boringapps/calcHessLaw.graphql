query calculateHessLaw (
    $reactants: [Float!]!,
    $products: [Float!]!,
) {
    calculateHessLaw (
        input: {
            reactants: $reactants,
            products: $products
        }
    )
}
