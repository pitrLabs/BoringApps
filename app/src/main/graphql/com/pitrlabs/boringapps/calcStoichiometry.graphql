query CalculateStoichiometry (
    $input: StoichiometryInput!
) {
    calculateStoichiometry (
        input: $input
    ) {
        product
        ratios {
            formula
            ratio
        }
    }
}
